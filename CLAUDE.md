# PU_Identification プロジェクト（再構想版 v2）

## 研究概要

SU（UAV）自身が受信信号から PU（ETC）を認知し、PUが使用中の周波数を特定して、
そこを避けて干渉を与えない周波数を選ぶ —— 認知無線によるUAV緊急ネットワークの動的スペクトルアクセス。
PLATEAUの3D都市環境でレイトレーシングを行い、現実的な伝搬条件下で識別性能を検証する。
最終的に既存のLBT-MAB（Thompson Sampling）に統合する。

---

## 識別の主体と目的（明確化）

```
SUが受信 → 拾った信号が PU(ETC) か 他のSU(UAV) かを判別
        → PUが居る周波数を特定 → そこを避けて干渉しない周波数を選択
```

**なぜPUとSUを「区別」する必要があるのか（研究の動機）:**
RSSIだけでは「電波がある」ことしか分からず、それがPUか他SUか区別できなかった（前回の限界）。
他のSUの信号を誤ってPUと判定すると、本当はPUが居ないのに周波数を回避し、貴重な帯域を無駄にする。
ゆえに「PUか否か」の判別が必須。

---

## システム設計

### PU（一次ユーザー）：ETC路側機
- 規格：ARIB STD-T75（DSRC）  ※IEEE 802.11p は米国系・別物。混同しないこと
- 周波数：5.8GHz帯　下り 5775–5805MHz（7ch）／上り 5815–5845MHz（7ch）、占有帯域幅 4.4MHz
- 変調：ASK（振幅偏移変調）※DSRC-VICS等の他用途はπ/4 QPSKだが、ETC本体はASK
- 同期パターン：規格で Unique Word が定義 → プリアンブル相関の前提が現実的に成立
- アンテナ：patchMicrostrip（物理モデル）、車道方向への固定指向性
- 送信：車両通過時のバースト通信

### SU（二次ユーザー）：UAV
- 変調：OFDM（サブキャリアは QPSK / 16QAM）
  - 注：OFDMは多重化方式、QPSK/QAMはサブキャリア変調。階層関係（並列ではない）
- 周波数：5.82GHz（DSRC帯域5770〜5850MHz内の空きチャネル）
- アンテナ：dipoleFolded（半波長ダイポール、物理モデル）※完全無指向性ではなくドーナツ型放射
- チャネル選択：LBT + Thompson Sampling（後フェーズで統合）

---

## 周波数シナリオ（プランB・選択肢2）確定

- ETC(PU)：5770〜5850MHz、7チャネル構成のDSRC帯域
- UAV(SU)：同じDSRC帯域内の空きチャネルを動的借用（5.82GHz）
- ISMバンド全体は5725〜5875MHz

シナリオ：UAVはETCと同一のDSRC帯域を共有し、ETCが通信中の
チャネルを識別して避け、空いているチャネルを認知無線的に借用する。

研究の位置づけ：日本では5.8GHz帯はETC専用だが、海外では
既にドローン利用可能で、国内でも解禁議論が総務省で進行中。
将来の解禁を見据え、同一帯域内でETCを識別・回避して共存する
PU識別技術として位置づける。

既存テーマとの接続：チャネル単位の借用はLBT-MAB
（Thompson Samplingによるチャネル動的選択）と直結する。
フェーズ5で識別結果をLBT-MABに統合する。

---

## SU/PU識別手法

### 主軸（アンテナ1本で計算可能・反射に強い）
| 特徴量 | 内容 | 備考 |
|---|---|---|
| 尖度（Kurtosis） | ASK=振幅が離散的、OFDM=ガウス的 で差が出る | SNRが下がると劣化（後述） |
| プリアンブル相関 | ETCの Unique Word との照合 | マルチパスでも相関ピークは残りやすい |

### 固定重み結合(0.5:0.5)の構造的欠陥（3回実証済み）

混信・マルチパス・複合の3条件すべてで同じ破綻パターンを確認：
- 尖度が崩壊（スコア≒0）するとき、結合スコア = 0.5×0 + 0.5×プリアンブル = 0.5×p
- 閾値0.5に正確に張り付き、わずかな数値ばらつきで判定が割れる
- 結果：プリアンブル単独100%に対して結合50%（逆転）

**正しい結合の形はカスケード（相関優先→尖度で補助）。固定重みは使わない。**
→ フェーズ3で改良予定（OR論理 / 適応重み / カスケード 3案比較）

### AoAは「別建ての比較研究」に切り出す
AoA（到来角）推定には複数素子アレイが必要で、ダイポール1本では不可能。
→ 識別の主軸からは外す。代わりに以下を独立した比較研究テーマとする：

比較シミュレーション計画A：アレイ素子数 vs 識別精度 vs 物理コスト
- 素子数 1 → 2 → 4 → 8 と増やしてAoA識別精度をMATLABでシミュレーション
- 5.8GHzの波長 ≈ 5.2cm、λ/2素子間隔 ≈ 2.6cm → 4素子でも全長8cm弱でドローン搭載可能
- ボトルネックはアンテナ素子でなく受信機チェーン（LNA/ミキサ/ADC）の系統数
  → 重量増・消費電力増（特にADC）・コスト増
- 精度はシミュレーション、重量・消費電力はカタログ値/文献値で見積もり → 総合判断
- アウトプット：「消費電力に見合う素子数の落としどころ」を定量的に示す

---

## 重要な知見：各手法の弱点と強み

### 尖度（Kurtosis）の弱点（3系統で実証済み）
| 劣化要因 | 原因 | 90%維持限界 |
|---|---|---|
| SNR低下（遮蔽） | 雑音がガウス化 | SNR ≈ 0dB |
| 干渉UAV | OFDM干渉がガウス化 | SIR ≥ +5dB |
| マルチパス | 多パス和がCLTでガウス化 | L ≤ 3 |

### プリアンブル相関の強靭性（3系統で実証済み）
| 条件 | 精度 | 備考 |
|---|---|---|
| SNR = -5dB | ~78%（PLATEAU NLOS） | 尖度は51%崩壊 |
| SIR = -5dB（干渉3倍） | **100%** | 尖度は50%崩壊 |
| L=5 マルチパス | **93%** | 尖度は81% |
| L=5 + SIR=0dB 複合 | **99.5%** | 尖度は50%崩壊 |

理論的根拠：プリアンブル相関＝整合フィルタ＝既知信号検出でSNR最大化（最適）。
UWテンプレートとランダム信号（OFDM干渉・雑音）との相関はほぼゼロ→閾値以下。

### 評価の主軸
**一次軸：SNR（遮蔽）**、**二次軸：SIR（混信）**、**三次軸：マルチパス数L**
研究の見せ場は低SNR×低SIR×高Lの複合条件でプリアンブルの優位性を示す点。

簡易シミュレーション（フェーズ3以前）の参考値（尖度のみ・8タップ・SNR変動）：
SNR≥0dB → 約90〜97% / SNR=-5dB → 約64% / SNR=-10dB → 約52%

---

## 実装フェーズ

### フェーズ1：信号モデルの構築
ETC信号（ASK）とUAV信号（OFDM）を生成。Toolbox不要、基本MATLABで動く。
※理想信号で識別できるのは当然。これは「コードが動く」確認であって成果ではない。

### フェーズ2：特徴量抽出
尖度・プリアンブル相関を計算するコードを実装。

### フェーズ3：識別器
特徴量を入力にした判定ロジック。まず閾値判定から。

比較シミュレーション計画B：手法 vs SNR ✅ 完了（compare_methods.m）

#### 比較実験C: 同一チャネル混信（compare_sir.m）✅ 完了（2026-06-06）

**実験条件:** AWGN SNR=20dB固定、UAV干渉電力を変化
- 問い1（SIR sweep）：正例=ETC+1台UAV干渉、負例=UAVのみ
- 問い2（干渉台数）：SIR=0dB/台固定、n_UAV=1,2,3,5,10台

**問い1 結果（SIR vs 精度）:**
| SIR [dB] | 尖度 | プリアンブル | 結合 |
|---|---|---|---|
| +5 | 0.998 | **1.000** | **1.000** |
| 0 | 0.500 | **1.000** | **1.000** |
| -5 | 0.500 | **1.000** | 0.507 |
| -10 | 0.500 | 0.620 | 0.500 |
| -20 | 0.500 | 0.500 | 0.500 |

90%維持限界SIR：尖度=+5dB / プリアンブル=**−5dB** / 結合=0dB

**問い2 結果（干渉台数 vs 精度、SIR=0dB/台）:**
| n_UAV | 実効SIR | 尖度 | プリアンブル | 結合 |
|---|---|---|---|---|
| 5 | -7.0dB | 0.500 | **1.000** | 1.000 |
| 10 | -10.0dB | 0.500 | **0.877** | 1.000 |

**主要な知見:**
1. プリアンブルはSIR=−5dB（干渉3倍）でも100%維持 → 最強の干渉耐性
2. 尖度はSIR=0dBで完全崩壊（OFDM干渉がASKをガウス化）
3. 問い1と問い2で結合の挙動が逆転：単一強干渉(問い1)で破綻、多数弱干渉(問い2)で良好
   → 信号長の非対称性（ETC=11968サンプル > UAV_resampled=7040サンプル）による末尾純ETC部分が影響
4. 課題：位相・タイミングは同期加算で簡略化（実際はランダム位相での検証が必要）

#### 比較実験D: マルチパス検証（compare_multipath.m）✅ 完了（2026-06-06）

**モデル:** タップ遅延線、fs=44MHz、τ_max=220サンプル（5μs=5ビット幅）
- 直接波τ=0、反射波τ∈[1,220]ランダム、複素ガウスタップ（レイリー振幅）
- 電力プロファイル：指数減衰 exp(-l/decay)、全パス正規化

**主実験（SNR=20dB、パス数 vs 精度）:**
| L_paths | 尖度 | プリアンブル | 結合 |
|---|---|---|---|
| 1 | 1.000 | 1.000 | 1.000 |
| 3 | 0.927 | **0.980** | 0.968 |
| 5 | 0.812 | **0.930** | 0.923 |
| 10 | 0.605 | **0.807** | 0.753 |

90%維持：尖度=L=3まで / プリアンブル=**L=5まで** / 結合=L=5まで

**副実験（マルチパスL=5 + 干渉UAV 複合条件）:**
| SIR | 尖度 | プリアンブル | 結合 |
|---|---|---|---|
| 0dB | 0.500 | **0.995** | 0.900 |
| -5dB | 0.500 | **0.915** | 0.500 |

**主要な知見:**
1. マルチパス自体が尖度を壊す（L=10で60.5%）― CLTによるガウス化が原因
2. プリアンブルはL=10でも80.7%維持 ― 相関ピークがにじんで低くなるだけで残存
3. 複合条件（L=5 + SIR=0dB）でプリアンブル99.5% → 最悪条件でも実用レベル
4. 固定重み結合の欠陥を複合条件で再確認（SIR=−5dBで結合50%、プリアンブル91.5%）

### フェーズ4：伝搬環境を段階的に導入（重要：いきなりフル都市に飛ばない）
```
① 自由空間（直接波のみ、反射ゼロ）         ✅ 完了
② 単純反射を1つ追加（地面2波モデル）        ✅ 完了
③ フル都市（PLATEAU）                      ✅ ステップ1完了
```
段階的にやることで「どの伝搬現象が識別を壊すか」を切り分け（アブレーション実験）。
※PLATEAUデータは plateau.mlit.go.jp から取得（CityGML）

#### フェーズ4③ ステップ1 完了済み結果（2026-06-06）

**シミュレーション設定:**
- ETC路側機: 座標(−250, −300) m、高さ5m、5.80GHz、Ptx=10dBm、BW=4.4MHz
- UAV受信機: 高度100m、50×50格子、ETC中心±500m四方（計2500点）
- 壁透過損: 12 dB/棟（コンクリート、文献値）
- 未実装（ステップ2以降）: 地面反射・建物反射・アンテナ指向性

**LOS/NLOS分布:**
| 状態 | 割合 | 説明 |
|---|---|---|
| LOS（0棟貫通） | **28.8%** | 道路・広場などの開空間 |
| NLOS（1棟） | 20.2% | 建物1棟を透過 |
| NLOS（2棟） | 19.6% | |
| NLOS（3棟以上） | 31.5% | 多重遮蔽 |

**識別精度（3手法）:**
| 手法 | 全体平均 | LOS | NLOS |
|---|---|---|---|
| 尖度のみ | 0.654 | **1.000** | 0.513 |
| プリアンブルのみ | 0.842 | **1.000** | 0.778 |
| **結合（推奨）** | **0.869** | **1.000** | **0.816** |

**ETC受信SNR:**
- LOS: 中央値 +15.1 dB（識別に十分）
- NLOS: 中央値 −13.9 dB（壁透過損が支配、3棟以上で−48 dB以下も）

**主要な知見:**
1. 都市建物配置が識別可能領域を空間的に決定する → LOS廊下（道路方向）に沿って識別可能ゾーンが伸びる
2. NLOS環境で初めて3手法に有意差が出た → 尖度単独はNLOSで崩壊（51%）、プリアンブルが粘り強い（78%）
3. 結合手法がNLOSでも最高精度（82%）→「2特徴量の組み合わせが核心」仮説を都市伝搬で実証
4. 90%ラインが空間的に明確 → 「どのUAV位置でETCを識別できるか」の運用境界を可視化

### フェーズ5：LBT-MABとの統合
識別結果をチャネル選択に組み込む。既存研究（PBA-TS by Amrallah et al., 2021）との差分を明確化。

---

## マルチホップ設計（後フェーズ）
ホップごとに異なるサブバンド使用可（SU1→SU2はサブバンド1、SU2→SU3はサブバンド2）。
中継ノードは送受で異なるサブバンドを使うため時分割設計が必要。
MABの報酬をホップ単位／経路単位のどちらにするかは要検討。

---

## フォルダ構成

```
PU_Identification/
├── signals/        gen_ETC.m / gen_UAV.m
├── features/       calc_kurtosis.m / calc_preamble.m
├── classifier/     identify.m
├── antenna/        antenna_PU.m（patch）/ antenna_SU.m（dipole）/ array_SU.m（比較研究用）
├── citymodel/      data/（CityGML置場）
│                   load_plateau.m          ✅ PLATEAU LOD1建物抽出
│                   check_plateau.m         ✅ 3D目視確認
│                   find_road_space.m       ✅ 道路空間特定マップ出力
│                   check_etc_pos.m         ✅ ETC設置座標の建物内外判定
├── raytracing/     raytrace_free_space.m   ✅ フェーズ4① 自由空間
│                   raytrace_two_ray.m      ✅ フェーズ4② 地面2波モデル
│                   raytrace_plateau.m      ✅ フェーズ4③ PLATEAU都市（ステップ1）
├── experiments/    compare_array.m（計画A）
│                   compare_methods.m       ✅ 計画B: 手法 vs SNR
│                   compare_sir.m           ✅ 比較C: 同一チャネル混信
│                   compare_multipath.m     ✅ 比較D: マルチパス耐性
└── main.m
```

---

## 未解決の検討事項

**ツールボックス確認済み (R2026a):**
- ✅ Communications Toolbox / Signal Processing Toolbox / Statistics and Machine Learning Toolbox
- ✗ Antenna Toolbox / Phased Array System Toolbox（なし）
- → MATLAB raytrace 関数は Antenna Toolbox なしではカスタム3D環境に使用不可。幾何的LOS/NLOS方式で代替

**フェーズ4③ ステップ2以降の課題:**
- 地面反射（2波モデル）のPLATEAU統合：ETC高さ5m・UAV高さ100mで2波干渉縞がLOS/NLOSマップにどう重なるか
- 建物反射（1次反射）の追加：壁面をミラーとした反射パスの計算
- アンテナ指向性の影響：ETCパッチアンテナ（車道向き固定）でUAV方向の利得損をモデル化

**今後の課題（優先順）:**
1. **結合手法の改良**（最優先）：固定重み0.5:0.5の欠陥を解決する3案を比較実装
   - OR論理：どちらか一方が閾値超えで検出
   - 適応重み：SNR推定値に応じてリアルタイム重み変更
   - カスケード：相関ピーク確認→尖度で補助（整合フィルタ理論に忠実）
2. **位相ランダム化・CFO補償**：現在は同期加算簡略化→実際はランダム位相での相関評価
3. **PSLR（ピーク対サイドローブ比）・複数区間一貫性**の活用
4. **フェーズ5**：識別結果をLBT-MABに統合（PBA-TS by Amrallah et al., 2021との差分明確化）

**見落とし手法メモ（文献調査より）:**
- サイクロステーショナリ検出：波形未知でも可だが計算量大。本研究はUW既知なので整合フィルタ優先で正当

**研究全体の未解決:**
- UAVの高度・移動パターンの設定（静止 vs 経路飛行）
- ETC車両通過頻度・密度の模擬方法
- 受信機のDC除去/AGCが尖度に与える影響（未実装）
- 新規性の軸：「PLATEAU実地形+ETC+2特徴量+空間的識別可能域マップ」の組み合わせで主張するか確認要

---

## 注意事項
- Thompson Sampling・LBT-MABの実装は後フェーズ（フェーズ5）
- ビームフォーミングは比較研究A（アレイ）の文脈でのみ登場、主軸では対象外
- フェーズ1〜3はPLATEAU・レイトレーシングなしで進める
- 「理想信号で識別成功」を成果と早合点しない。検証価値はフェーズ4（特に低SNR）にある
